<h1>All Players in the Playoff Race</h1>
  <% @players.each do |player| %>
    <%= link_to player.name, player_path(player.slug) %>: <%= player.status %> with <%= player.teams.first.name %><br/>
    <%# binding.pry %>
  <% end %>
